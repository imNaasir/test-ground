
export const handler = (type: string, date: unknown) => {};

// const transactionHandler = () => {};

// // request payment
// if (content.data.type === "request-payment") {
//     const { type, walletAddress, name, currency, amount, confirm, decline, image } = content.data as RequestPaymentContentProps;
//     // navigate("RequestScreen", { walletAddress, name, currency, amount, confirm, decline, imgUri: image });
//     navigate("AppStack", { screen: "NotFound" });
// }

// if (content.data.type === "transaction") {
//     const { trxnId, trxType } = content.data.data as any;

//     // transfer in
//     if (trxType === "transfer in") {
//         // navigate("TransactionDetailScreen", { transaction_id: trxnId as string });
//     }
// }
